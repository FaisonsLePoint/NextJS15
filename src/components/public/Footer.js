import style from "@styles/public/footer.module.css"

const Footer = () => {
    return (
        <footer className={style.footer}>
            Le Footer !
        </footer>
    );
}

export default Footer;